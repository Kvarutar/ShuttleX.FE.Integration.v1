import { Image, type ImageStyle } from 'react-native';

const BusinessXWheel = ({ style }: { style?: ImageStyle }) => <Image source={require('./Wheel.png')} style={style} />;

export default BusinessXWheel;
