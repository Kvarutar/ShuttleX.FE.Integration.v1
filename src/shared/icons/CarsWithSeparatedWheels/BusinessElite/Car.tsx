import { Image, type ImageStyle } from 'react-native';

const BusinessEliteCar = ({ style }: { style?: ImageStyle }) => <Image source={require('./Car.png')} style={style} />;

export default BusinessEliteCar;
