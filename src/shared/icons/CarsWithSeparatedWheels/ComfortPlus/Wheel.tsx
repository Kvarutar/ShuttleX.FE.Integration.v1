import { Image, type ImageStyle } from 'react-native';

const ComfortPlusWheel = ({ style }: { style?: ImageStyle }) => <Image source={require('./Wheel.png')} style={style} />;

export default ComfortPlusWheel;
